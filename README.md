
{
  "меню": {
         "welcome_text": "👋Привет! Бог случайности поможет вам создать раздачу на канале.\n Вы готовы создать новую раздачу?",
         "menu_buttons": ["Создать розыгрыш 🎁", "Мои розыгрыши 🎉", "Сменить язык 🇷🇺", "Поддержать ⁉️", "Поделиться с друзьями 👥"],
         "пригласить": "Поделитесь ботом со своими друзьями, отправив им имя пользователя бота \n @RandomGodBot",
         "поддержка": "t.me/FeedbackRandomGodBot"
        },
  "розыгрыш": {

         "back_in_menu": "Вернуться в меню ↩️"",
         "назад": "Назад ↩️",
         "draw_buttons": ["изменение времени начала ⏳", "изменение времени окончания ⌛️", "измените кол-во победителей 🏆", "изменить описание 📑", "изменить фото/гиф 🖼", "проверить подписку ✅", "публикации 🎲", "назад в меню ↩️"],

         "chanel_id":"Введите имя пользователя канала, на котором будет опубликован розыгрыш в формате @username. Вы должны быть администратором этого канала и добавить бота в список администраторов. Если канал приватный, то вам нужно отправить идентификатор этого канала, чтобы узнать идентификатор приватного канала, перешлите любой пост с этого канала специальному боту @getmyid_bot, суть в ответе от бота и будет идентификатором вашего приватного канала. Перешлите этот идентификатор боту",
         "chanel_id_check": "Введите имя пользователя канала для проверки в формате @username. \n Если канал приватный, то вам нужно отправить идентификатор этого канала, чтобы узнать идентификатор приватного канала, перешлите любой пост с этого канала специальному боту @getmyid_bot, суть в ответе от бота и будет идентификатором вашего приватного канала. Перешлите этот идентификатор боту. Укажите ссылку на этот канал в описании, чтобы участники розыгрыша знали, на какие каналы вам нужно подписаться",
         "not_admin":"Для публикации розыгрыша вы должны быть администратором этого канала. Отправить имя пользователя от имени администратора",
         "not_in_chanel":"Бот не является администратором этого канала. Добавьте бота в администраторы и повторно отправьте @username",

         "draw_text":"Введите описание розыгрыша. \n Совет: опишите, какие будут призы, сколько будет победителей и что выиграет каждый из них, на какие каналы нужно подписаться участникам, а также укажите дату и время окончания розыгрыша. Вы можете использовать html-разметку.\n 1)Жирный текст - <b>ваш текст</b>\n 2)Курсивный текст - <i> ваш текст</i>\n 3) Гиперссылка на канал - <a href='ссылка на ваш канал'>ваш текст</a>.\n Ссылка должна быть в таком формате: https://t.me/channel ",
         "файл":"Отправьте картинку или GIF (необязательно, если не нужно, отправьте любое другое сообщение)",

         "winers_count":"Введите количество победителей",
         "not_int": "Это не число",

         "post_time":"Введите дату и время начала розыгрыша в формате: (год-месяц-день, часы: минуты), например 2020-10-25 21:05",
         "end_time":"Введите дату и время окончания розыгрыша в формате: (год-месяц-день, часы: минуты), например 2020-10-31 21:55",
         "over_time": "Это время уже истекло, введите время, которое будет больше текущего момента",
         "invalid_format_time": "Неверный формат времени, введите время в формате: (год-месяц-день, часы: минуты), например 2020-08-25 21:00",
         "post_biger": "Время публикации больше, введите время в формате: (год-месяц-день, часы: минуты), например 2020-08-26 21:41",

         "get_on":"Участвовать",

         "change_text": "Что вам нужно изменить в раздаче?",
         "chanel/чат": "Канал /чат:",
         "текст": "Описание:",
         "count_text":"Количество победителей:",
         "post_time_text":"Время начала:",
         "over_time_text":"Время окончания:",

         "submit_text":"Розыгрыш создан и будет опубликован на канале в выбранное время",


         "играть":"Участвовать",
         "not_subscribe": "@RandomGodBot: Вы подписаны не на все каналы, пожалуйста, внимательно прочитайте описание розыгрыша и подпишитесь на все необходимые каналы для участия",
         "already_in":"@RandomGodBot: Вы уже приняли участие",
         "got_on":"@RandomGodBot: Поздравляем, вы участвовали в розыгрыше!",

         "победители":"Розыгрыш закончился. Победители:",
         "no_winers":"Победителей нет",

         "failed_post": "Не удалось отправить раздачу, потому что канал был удален",
         "your_draw_over": "Розыгрыш окончен"
        },

  "my_draw":{
         "no_draw": "У вас нет активных раздач",
         "последний":"Последний",
         "первый":"Следующий",
         "your_draw":"Ваш розыгрыш:",
         "chanel/чат": "Канал /чат:",
           "текст": "Описание:",
           "count_text":"Количество победителей:",
           "post_time_text":"Время запуска:",
           "over_time_text":"Время окончания:",

           "следующий":"Следующий",
           "назад":"Назад"

        }
        @@ -0,0 +1,69 @@
{
  "меню": {
  		   "welcome_text": "👋Приветствуем!\n Бог рандома поможет провести Вам розыгрыш на канале.\nГотовы создать новый розыгрыш?",
  		   "menu_buttons": ["Создать конкурс 🎁", "Мои конкурсы 🎉", "Change language 🇬🇧", "Поддержка ⁉️", "Пригласить друзей 👥"],
  		   "invite": "Поделитесь ботом с друзьями, отправив им юзернейм бота\n @RandomGodBot",
  		   "поддержка": "t.me/FeedbackRandomGodBot"
  		  },
  "розыгрыш": {

  		   "back_in_menu": "Назад в главное меню ↩️",
  		   "back": "Назад ↩️",
  		   "draw_buttons": ["Изменить время начала ⏳", "Изменить время окончания ⌛️", "Количество победителей 🏆", "Изменить описание 📑", "Изменить фото/gif 🖼", "Проверить подписку ✅", "Опубликовать 🎲", "Назад в главное меню ↩️"],

  		   "chanel_id":"Введите юзернейм канала, в котором будет опубликован розыгрыш в формате @username. Вы должны быть администратором этого канала и добавить в администраторы бота.\n Если канал приватный, то необходимо отправить id этого канала, чтобы узнать id приватного канала, перешлите любой пост из этого канала специальному боту @getmyid_bot, нижняя строка-ответ от бота и будет являться id Вашего приватного канала. Перешлите этот id боту",
  		   "chanel_id_check": "Введите юзернейм канала для проверки в формате @username.\n Если канал приватный, то необходимо отправить id этого канала, чтобы узнать id приватного канала, перешлите любой пост из этого канала специальному боту @getmyid_bot, нижняя строка-ответ от бота и будет являться id Вашего приватного канала. Перешлите этот id боту.\n Укажите ссылку на этот канал в описании, чтобы участники розыгрыша знали на какие каналы нужно подписаться, чтобы участвовать в нем",
  		   "not_admin":"Для того, чтобы разместить пост о розыгрыше, Вы должны быть администратором данного канала. Пришлите username от лица администратора",
  		   "not_in_chanel":"Бот не является администратором данного канала. Добавьте бота в администраторы и отправьте @username ещё раз",

  		   "draw_text":"Введите описание розыгрыша.\n Совет: опишите что Вы разыгрываете, сколько будет победителей и что каждый из них выиграет, на какие каналы необходимо подписаться участникам, и укажите дату и время окончания розыгрыша.\n Вы можете использовать html разметку:\n 1)Жирный текст - <b>Ваш текст</b>\n 2)Курсив - <i>Ваш текст</i>\n 3)Гиперссылка на канал - <a href='Ваша ссылка на канал'>Ваш текст</a>.\n Ссылка должна быть в формате: https://t.me/channel",
  		   "file":"Отправьте картинку или гифку (опционально, если не нужно, отправьте любое другое сообщение)",

  		   "winers_count":"Введите количество победителей",
  		   "not_int": "Это не число",

  		   "post_time":"Введите дату и время начала розыгрыша в формате: (год-месяц-день часы:минуты), например 2020-10-25 21:05",
  		   "end_time":"Введите дату и время окончания розыгрыша в формате: (год-месяц-день часы:минуты), например 2020-10-31 22:50",
  		   "over_time": "Это время уже вышло, введите время которое будет больше текущего момента",
  		   "invalid_format_time": "Не верный формат времени, введите время в формате: (год-месяц-день часы:минуты), например 2020-08-26 21:41",
  		   "post_biger": "Время постинга больше, ведите время в формате: (год-месяц-день часы:минуты), например 2020-08-26 21:41",

  		   "get_on":"Участвовать",

  		   "change_text": "Что вам необходимо изменить в конкурсе?",
  		   "chanel/chat": "Канал/чат:",
  		   "text": "Описание:",
  		   "count_text":"Количество победителей:",
  		   "post_time_text":"Время начала розыгрыша:",
  		   "over_time_text":"Время окончания розыгрыша:",

  		   "submit_text":"Розыгрыш создан и будет опубликован на канале в указанное время",


  		   "play":"Участвовать!",
  		   "not_subscribe": "@RandomGodBot: Вы не подписались на все каналы, внимательно прочтите условия розыгрыша и подпишитесь на все необходимые каналы, чтобы участвовать",
  		   "already_in":"@RandomGodBot: Вы уже приняли участие",
  		   "got_on":"@RandomGodBot: Поздравляю, вы приняли участие в конкурсе!",

  		   "winers":"Конкурс завершен.\n Победители:",
  		   "no_winers":"Победителей нет!",

  		   "failed_post": "Не удалось отправить розыгрыш, т.к канал был удален",
  		   "your_draw_over": "Ваш розыгрыш закончен"
  		  },

  "my_draw":{
  			 "no_draw": "Активных розыгрышей нет",
  			 "last":"Последний",
  			 "first":"Первый",
  			 "your_draw":"Ваш розыгрыш:",
  			 "chanel/chat": "Канал/чат:",
  		   	 "text": "Описание:",
  		   	 "count_text":"Количество победителей:",
  		   	 "post_time_text":"Время начала розыгрыша:",
  		   	 "over_time_text":"Время окончания розыгрыша:",

  		   	 "next":"Вперед",
  		   	 "back":"Назад"

  			}
@@ -0,0 +1,14 @@
импорт telebot
импорт конфигурации
из базы импортировать базу
из fsm импортировать FSM


telebot = бот.TeleBot(конфигурация.ТОКЕН)
База данных = fsm_base()
База данных = middleware_base()
База данных = main_base()
База данных = tool_base()
База данных = post_base()
База данных = end_base()
FSM = fsm(fsm_base)
@@ -0,0 +1,71 @@
из модели импортируют сессию
из моделей импортируйте базу как BaseObj
из моделей импортируйте движок как базовый двигатель
из sqlalchemy import select




База данных класса:
	выберите_all def(self,Model, **filter_s):
		сессия = запрос.запрос(модель)
		len if(filter_s) > 0:
			запрос = query.filter_by(**filter_s)
		запрос возвращает.все()


	get_one def(self,Model, **filter_s):
		сессия = запрос.запрос(модель)
		len if(filter_s) > 0:
			запрос = query.filter_by(**filter_s)
		запрос возвращает.first()


	тест определения(self,Model, **filter_s):
		self if.get_one(Модель,**filter_s):
			возвращает True
		ещё:
			возвращает False


	новое определение(self,модель, *аргументы): 
		Модель = tmp_new(*аргументы)
		сессия.добавить(tmp_new)
		сессия.фиксация()
		вернуть tmp_new


	удалить def(self,Model, **filter_s):
		self = obj.select_all(Модель,**filter_s)
		возражаю , если:
			объект в i для:
				сессия.удалить(я)
			сессия.фиксация()
			возвращает True
		ещё:
			возвращает False


	обновите определение(себя, модель, набор, **filter_s):
		сессия = запрос.запрос(модель)
		len if(filter_s) > 0:
			запрос = query.filter_by(**filter_s)
		запрос.обновление(установлено)
		сессия.фиксация()
		возвращает True


	set_state def(self, Model, *аргументы):
		Модель = to_byte(*аргументы)
		сессия.добавить(to_byte)
		сессия.фиксация()


	base_init def(self):
		BaseObj.метаданные.create_all(BaseEngine) 



База данных = d()
d.base_init()
распечатать('ok')
ТОКЕН = "
db_url = "
канал = "
 19 изменений: 19 добавлений и 0 удалений19  
 импорт моделей
импорт pickle

FSM класс():
	__init__ def(self, база):
		self.base = база

	set_state def(self, user_id, state, **arg):
		self.база.удалить(модели.Состояние, user_id=user_id)
		pickle = данные.дампы(аргументы)
		self.база.set_state(модели.Состояние, идентификатор пользователя, состояние, данные)

	get_state def(self, user_id):
		self = tmp.база.select_all(модели.Состояние, user_id=user_id)
		tmp if:
			pickle = arg.загружается(tmp[0].arg)
			возврат (tmp[0].состояние, аргумент)
		ещё:
			возврат ("бездействующий", [])
   импорт json
импорт telebot
language_check импортирует инструмент из, create_inlineKeyboard

get_menu_keyboard определение(user_id):
	проверка языка = кнопки(идентификатор пользователя)[1]['menu']['menu_buttons']
	telebot = menu_keyboard.типы.ReplyKeyboardMarkup(resize_keyboard=True)
	menu_keyboard.строка(кнопки[0], кнопки[1])
	menu_keyboard.строка(кнопки[2])
	menu_keyboard.строка(кнопки[3], кнопки[4])
	вернуть menu_keyboard


get_draw_keyboard определение(user_id):
	проверка языка = buttons(user_id)[1]['draw']['draw_buttons']
	telebot = draw_keyboard.типы.ReplyKeyboardMarkup(resize_keyboard=True)
	draw_keyboard.строка(кнопки[0], кнопки[1])
	draw_keyboard.строка(кнопки[2], кнопки[3])
	draw_keyboard.строка(кнопки[4], кнопки[5])
	draw_keyboard.строка(кнопки[6], кнопки[7])
	вернуть draw_keyboard


back_button def(user_id):
	проверка языка = кнопки(идентификатор пользователя)[1]['рисовать']['возвращаться']
	telebot = back_button.типы.ReplyKeyboardMarkup(resize_keyboard=True)
	back_button.строка(кнопки)
	вернуть back_button

импорт времени
импорт моделей
импорт случайного
импорт потоков
импорт клавиатуры
language_check импортирует инструмент из, create_inlineKeyboard
middleware_base импортирует приложение из, бота, post_base, end_base 
из даты и времени импортировать дату и время
из даты и времени импортируйте timedelta




check_user def(user_id):
	промежуточная база = user.get_one(модели.Пользователь, user_id=str(user_id))
	Нет != пользователь если:
		вернуть пользователя
	ещё:
		возвращает False


create_draw_progress def(user_id, tmp):
	промежуточная база.удалить(модели.DrawProgress, user_id=(str(user_id)))
	база промежуточного программного обеспечения.новые(модели.DrawProgress, str(user_id), tmp['chanel_id'], tmp['chanel_name'], tmp['draw_text'], tmp['file_type'], tmp['file_id'], int(tmp['winers_count']), tmp[' start_time'], tmp['end_time'])
	промежуточная база.удалить(модели.Состояние, user_id=str(user_id))

	draw_info возвращает(user_id)






draw_info def(user_id):
	check_post = tmp(str(user_id))
	проверка языка = текст(идентификатор пользователя)[1]['ничья']
	draw_text = f"  tmp.время окончания}\n{текст['chanel/chat']} { .winers_count}\n{текст['text']}.  {tmp.текст}"


check_post определение(user_id):
	промежуточная база данных = данные.get_one(модели.DrawProgress, user_id=str(user_id))

отправь_драйв_инфо определение(user_id):
	check_post = tmp(str(user_id))
	проверка языка = текст(идентификатор пользователя)[1]['ничья']
	draw_text = f"  tmp.время окончания}\n{текст['chanel/chat']} { .winers_count}\n{текст['text']}.  {tmp.текст}"
	tmp if.file_type == 'photo':
		бот.отправь_фото(user_id, tmp.file_id, draw_text, reply_markup=клавиатура.get_draw_keyboard(user_id))
	tmp if.file_type == 'document':
		бот.отправит_документ(user_id, tmp.file_id, заголовок=draw_text, reply_markup=клавиатура.get_draw_keyboard(user_id))
	ещё:
		бот.отправить сообщение(user_id, draw_text, reply_markup=клавиатура.get_draw_keyboard(user_id))
	промежуточная база.удалить(модели.Состояние, user_id=user_id)



def my_draw_info(user_id, row=0):
	if row < 0:
		return 'first'

	text = language_check(user_id)[1]['my_draw']
	notposted = middleware_base.select_all(models.DrawNot, user_id=str(user_id))
	posted = middleware_base.select_all(models.Draw, user_id=str(user_id))
	all_draws = notposted + posted
	if len(all_draws) == 0:
		bot.send_message(user_id, text['no_draw'])

	if row >= len(all_draws):
		print('notttt')



	draw_text = f"{text['your_draw']}\n{text['post_time_text']} {all_draws[row].post_time}\n{text['over_time_text']} {all_draws[row].end_time}\n{text['chanel/chat']} {all_draws[row].chanel_name}\n{text['count_text']} {all_draws[row].winers_count}\n{text['text']} {all_draws[row].text}"
	keyboard = create_inlineKeyboard({text['back']: "back", text['next']: "next"}, 2)
	if all_draws[row].file_type == 'photo':
		bot.send_photo(user_id, all_draws[row].file_id, draw_text, reply_markup=keyboard)
	elif all_draws[row].file_type == 'document':
		bot.send_document(user_id, all_draws[row].file_id, caption=draw_text, reply_markup=keyboard)
	else:
		bot.send_message(user_id, draw_text, reply_markup=keyboard)





def start_draw_timer():
	def timer():
		while 1:
			for i in post_base.select_all(models.DrawNot):

				count = 0
				post_time = datetime.now().strftime('%Y-%m-%d %H:%M')
				post_time = time.strptime(post_time, '%Y-%m-%d %H:%M')
				if post_time >= time.strptime(i.post_time, '%Y-%m-%d %H:%M'):
					if i.file_type == 'photo':
						tmz = bot.send_photo(i.chanel_id, i.file_id, i.text, reply_markup=create_inlineKeyboard({language_check(i.user_id)[1]['draw']['get_on']:f'geton_{i.id}'}))
					elif i.file_type == 'document':
						tmz = bot.send_document(i.chanel_id, i.file_id, caption=i.text, reply_markup=create_inlineKeyboard({language_check(i.user_id)[1]['draw']['get_on']:f'geton_{i.id}'}))
					ещё:
						бот = tmz.отправить сообщение(я.chanel_id, я.text, reply_markup=create_inlineKeyboard({language_check(я.user_id)[1]['draw']['get_on']:f'geton_{ я.id}'}))
					post_base.новые(модели.Розыгрыш, i.id, i.user_id, tmz.message_id, i.chanel_id, i.chanel_name, i.text, i.file_type, i.file_id, i.winers_count, i.post_time, i. время окончания)
					post_base.удалить(модели.Не нарисовано, id=str(i.id))
			время.сон(5)
	многопоточность = rT.Поток(цель = таймер)
	rT.начать()


end_draw_timer def():
	end_timer def():
		1 пока:
			конечная база в я для.select_all(модели.Розыгрыш):
				количество = 0
				дата-время = post_time.сейчас().strftime('%Y-%m-%d %H:%M')
				время = post_time.strptime(post_time, '%Y-%m-%d %H:%M')
				time >= post_time если.strptime(i.end_time, '%Y-%m-%d %H:%M'):
					проверка языка = текст(i.user_id)[1]['ничья']
					конечная база = игроки.выбрать всех(модели.DrawPlayer, draw_id=str(i.id))
					== игроки если []:
						победители = f"{я.текст}\n****\n{текст['no_winers']}"
						owin = f"{текст['no_winers']}"
					ещё:
						победители = f"{я.текст}\n****\n{текст['winers']}\n"
						owin = f"{текст['winers']}\n"
						диапазон в x для(int(i.winers_count)):
							len >= количество если(игроки):
								перерыв
							random = random_player.выбор(игроков)
							победители += f"<a href='tg://user?id={random_player.user_id}'>{random_player.имя_пользователя}</a>\n"
							owin += f"<a href='tg://user?id={random_player.user_id}'>{random_player.имя_пользователя}</a>\n"
							количество += 1
					попробовать:
						бот.отправь_сообщение(chat_id=str(я.chanel_id), текст=победители, parse_mode='HTML')
					кроме:
						конечная база.удалить(модели.Розыгрыш, id = i.id)
						бот.отправить сообщение(я.chanel_id, текст['failed_post'])
						return 'gg'
					bot.send_message(i.user_id, f"{text['your_draw_over']}\n{owin}", parse_mode='HTML')
					end_base.delete(models.Draw, id = i.id)
					time.sleep(1)

			time.sleep(5)
	rT = threading.Thread(target = end_timer)
	rT.start()


def new_player(call):
	int = id(вызов.данные.разделить('_')[1])
	промежуточная база = tmp.get_one(модели.Розыгрыш, id=id)
	промежуточная база = chanel.выберите все(модели.Подписаться на канал, draw_id=tmp.id)
	= статус ['оставлен', 'удален', 'ограничен', 'участник', 'администратор', 'создатель']
	шанель в я за: 
		бот если.получит_chat_member(chat_id=я.channel_id, user_id=вызов.от_user.id).статус в статусе:
			возврат ('not_subscribe')

	промежуточная база = игроки.get_one(модели.DrawPlayer, draw_id=str(tmp.id), user_id=str(вызов.от_user.id))
	Нет == игроки , если:
		база промежуточного программного обеспечения.новые(модели.DrawPlayer, tmp.id, str(вызов.от_user.id), str(вызов.от_user.username))
		промежуточная база данных = tmz.select_all(модели.DrawPlayer, draw_id=tmp.id)
		return (len(tmz), language_check(tmp.user_id)[1]['draw']['play'])
	ещё:
		возвращает (False)
  импорт конфигурации
импорт времени
Таблица импортирует sqlalchemy из, метаданных, столбца, целого числа, строки, внешнего ключа, двоичного файла, логического
sqlalchemy из.orm import mapper
из sqlalchemy импортируем create_engine
sqlalchemy из.ext.декларативный импорт declarative_base
sqlalchemy из.orm import sessionmaker
sqlalchemy из.orm импортирует scoped_session

# ---ИНИЦИАЛИЗАЦИЯ---
create_engine = движок(config.db_url, echo=False)
scoped_session = сессия(создатель сессий(привязка=движок,автозапуск=False))
декларативная база = База()

Пользовательский класс(Базовый):
	__имя_таблицы__ = 'bot_user'
	Столбец = user_id(Строка, primary_key=True)
	Столбец = имя_пользователя(Строка)
	Столбец = язык(Строка)


	__init__ def(self, user_id, имя_пользователя, язык):
		self.user_id = user_id
		self.имя_пользователя = имя_пользователя,
		селфи.язык = language


	__repr__ def(self):
		% "<User(user_id='%s', user_name='%s', language='%s')>" вернуть (
								 self.user_id, self.имя_пользователя, self.язык)


Класс DrawProgress(Базовый):
	__имя_таблицы__ = 'draw_progress'
	Столбец = id(Целое число, primary_key=True)
	Столбец = user_id(Строка)
	Столбец = chanel_id(Строка)
	Столбец = имя_шанели(Строка)
	Столбец = текст(строка)
	Столбец = file_type(Строка)
	Столбец = file_id(Строка)
	Столбец = winers_count(Целое число)
	Столбец = post_time(Строка)
	Столбец = время окончания(Строка)


	__init__ def(self, user_id, chanel_id, chanel_name, text, file_type, file_id, winers_count, post_time, end_time):
		self.user_id = str(user_id)
		self.chanel_id = str(chanel_id)
		self.имя_шанели = имя_шанели
		self.текст = text
		self.file_type = file_type
		self.file_id = file_id
		self.winers_count = winers_count
		self.post_time = post_time
		self.end_time = end_time


	__repr__ def(self):
		% "<DrawProgress(id='%s', user_id='%s', chanel_id='%s', chanel_name='%s', text='%s', file_type='%s', file_id='%s', winers_count='%s', post_time='%s', end_time='%s')>" return (
								self.id,
								self.user_id,
								self.chanel_id,
								self.имя шанель,
								селфи.текст,
								self.file_type,
								self.file_id,
								self.winers_count,
								self.post_time,
								self.end_time)

Не нарисованный класс(Базовый):
	__имя_таблицы__ = 'не опубликовано'
	Столбец = id(Целое число, primary_key=True)
	Столбец = user_id(Строка)
	Столбец = chanel_id(Строка)
	Столбец = имя_шанели(Строка)
	Столбец = текст(строка)
	Столбец = file_type(Строка)
	Столбец = file_id(Строка)
	Столбец = winers_count(Целое число)
	Столбец = post_time(Строка)
	Столбец = время окончания(Строка)


	__init__ def(self, id, user_id, chanel_id, chanel_name, text, file_type, file_id, winers_count, post_time, end_time):
		self.id = id
		self.user_id = str(user_id)
		self.chanel_id = str(chanel_id)
		self.имя_шанели = имя_шанели
		self.text = текст
		self.file_type = file_type
		self.file_id = file_id
		self.winers_count = winers_count
		self.post_time = post_time
		self.end_time = end_time


	__repr__ def(self):
		% "<не опубликовано (id='%s', user_id='%s', chanel_id='%s', chanel_name='%s', text='%s', file_type='%s', file_id='%s', winers_count='%s', post_time='%s', end_time='%s')>" возврат (
								self.id,
								self.user_id,
								self.chanel_id,
								self.имя шанель,
								селфи.текст,
								self.file_type,
								self.file_id,
								self.winers_count,
								self.post_time,
								self.end_time)



Розыгрыш класса(Базовый):
	__имя_таблицы__ = 'draw_'
	Столбец = id(Целое число, primary_key=True)
	Столбец = user_id(Строка)
	Столбец = message_id(Строка)
	Столбец = chanel_id(Строка)
	Столбец = имя_шанели(Строка)
	Столбец = текст(строка)
	Столбец = file_type(Строка)
	Столбец = file_id(Строка)
	Столбец = winers_count(Целое число)
	Столбец = post_time(Строка)
	Столбец = время окончания(Строка)


	__init__ def(self, id, user_id, message_id, chanel_id, chanel_name, text, file_type, file_id, winers_count, post_time, end_time):
		self.id = id
		self.user_id = str(user_id)
		self.message_id = str(message_id)
		self.chanel_id = str(chanel_id)
		self.имя_шанели = имя_шанели
		self.текст = text
		self.file_type = file_type
		self.file_id = file_id
		self.winers_count = winers_count
		self.post_time = post_time
		self.end_time = end_time


	__repr__ def(self):
		% "<DrawProgress(id='%s', user_id='%s', message_id='%s', chanel_id='%s', chanel_name='%s', text='%s', file_type='%s', file_id='%s', winers_count='%s', post_time='%s', end_time='%s') >" возврат (
								self.id,
								self.user_id,
								self.message_id,
								self.chanel_id,
								self.имя шанель,
								селфи.текст,
								self.file_type,
								self.file_id,
								self.winers_count,
								self.post_time,
								self.end_time)






Класс подписного канала(Базовый):
	__имя_таблицы__ = 'канал'
	id = Column(Integer, primary_key=True)
	draw_id = Column(Integer)
	user_id = Column(String)
	channel_id = Column(String)


	def __init__(self, draw_id, user_id, channel_id):
		self.draw_id = draw_id
		self.user_id = user_id
		self.channel_id = channel_id


	def __repr__(self):
		return "<channel(id='%s', draw_id='%s', user_id='%s', channel_id='%s')>" % (
			self.id, self.draw_id, self.user_id, self.channel_id)



class DrawPlayer(Base):
	__tablename__ = 'players'
	id = Column(Integer, primary_key=True)
	draw_id = Column(Integer)
	user_id = Column(String)
	user_name = Column(String)


	def __init__(self, draw_id, user_id, user_name):
		self.draw_id = draw_id
		self.user_id = user_id
		self.user_name = user_name


	__repr__ def(self):
		% "<Игрок(draw_id='%s', user_id='%s', user_name='%s')>" вернуть (
			self.draw_id, self.user_id, self.имя_пользователя)



Состояние класса(Базовый):
	__имя_таблицы__ = 'user_state'
	Столбец = user_id(Целое число, primary_key=True)
	Столбец = состояние(Строка)
	Столбец = arg(Двоичный код)

	__init___ def(self, user_id, state, arg):
		self.user_id = user_id
		self.state = состояние
		self.arg = аргумент

	__repr__ def(self):
		% "<State(user_id='%s', state='%s', arg='%s')>" возвращает (
								 self.user_id, self.state, self.arg)


импорт json
импорт telebot
импорт моделей
из приложения импортировать tool_base




открыть = файл("RU.json", кодировка="utf-8")
json = ru_bot_text.загрузить(файл)

открыть = файл("ENG.json", кодировка="utf-8")
json = eng_bot_text.загрузить(файл)


# ----Подгружаем файл с текстами ответов бота----
language_check def(user_id):
    tool_base = язык.get_one(модели.Пользователь, user_id=str(user_id))
    Нет == язык если:
        возвращает (False, ru_bot_text)
    ещё:
        язык if.язык == "RU":
            возвращает (True, ru_bot_text)
        ещё:
            возвращает (True, eng_bot_text)


create_inlineKeyboard def(ключ, строка=0):
    telebot = клавиатура.типы.Встроенный keyboardmarkup()
    = список ключей []
    количество = 0
    ключ в i для:
        список ключей.добавить(telebot.типы.InlineKeyboardButton(
            i=текст, callback_data=ключ.получить(i)))
        количество += 1

        строка >= количество если:
            клавиатура.добавить(*[i для меня в списке клавиш])
            = список ключей []
            количество = 0
        список if(ключ.ключи())[-1] == я:
            клавиатура.добавить(*[i для меня в списке клавиш])
    вернуть клавиатуру
